import 'package:flutter/material.dart';

class BanquetProfile extends StatelessWidget {
  const BanquetProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BanquetProfile'),
      ),
    );
  }
}
